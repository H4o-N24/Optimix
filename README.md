# 🗓️ Knot - 次世代Discord日程調整Bot

**「イベント成功率を最大化する、条件付き日程最適化Bot」**

Knotは、メンバーの空きスケジュールに基づき、「誰が必須か」「最低何人必要か」という条件を満たす最適日をBotが自動的に提案するDiscord Botです。

## ✨ 主な機能

| 機能 | 説明 |
|:---|:---|
| 📅 **空き日登録** | 翌月の空き日をカレンダー選択UIで一括登録 |
| 🎯 **条件付きイベント作成** | 必須メンバー・最低人数・曜日フィルターを指定 |
| 🏆 **最適日自動提案** | 条件を満たす日をランキング形式で提示 |
| 👥 **定員管理** | 先着順参加・キャンセル待ち自動繰り上げ |

## 🚀 セットアップ

### 1. 環境要件

- Node.js >= 20
- npm >= 10

### 2. インストール

```bash
git clone https://github.com/H4o-N24/Optimix.git
cd Optimix
npm install
```

### 3. 環境変数の設定

```bash
cp .env.example .env
```

`.env` を編集し、以下を設定:

| 変数名 | 説明 |
|:---|:---|
| `DISCORD_TOKEN` | Discord Botのトークン |
| `CLIENT_ID` | Discord Application のClient ID |
| `GUILD_ID` | 開発用サーバーのID（任意） |
| `DATABASE_URL` | SQLiteデータベースのパス |

### 4. データベースの初期化

```bash
npx prisma migrate dev --name init
```

### 5. 起動

```bash
# 開発モード
npm run dev

# 本番モード
npm run build
npm start
```

## 📋 コマンド一覧

| コマンド | 説明 |
|:---|:---|
| `/availability` | 翌月の空き日を登録 |
| `/event create` | 条件付きイベントを作成し最適日を提案 |
| `/event list` | イベント一覧を表示 |
| `/event info` | イベントの詳細・参加者を表示 |
| `/help` | ヘルプを表示 |

### `/event create` のオプション

| オプション | 説明 | 必須 |
|:---|:---|:---|
| `title` | イベント名 | ✅ |
| `min` | 最低参加人数 | - |
| `max` | 定員（上限） | - |
| `required1〜3` | 必須メンバー | - |
| `dayfilter` | 平日のみ / 週末のみ | - |

## 🏗️ 技術スタック

- **Runtime**: Node.js (TypeScript)
- **Discord**: discord.js v14
- **ORM**: Prisma + SQLite
- **Test**: Vitest

## 📁 プロジェクト構成

```
Knot/
├── docs/           # 設計ドキュメント
├── src/
│   ├── commands/   # スラッシュコマンド
│   ├── events/     # Discordイベントハンドラ
│   ├── services/   # ビジネスロジック
│   └── utils/      # ユーティリティ
├── prisma/         # DBスキーマ
├── tests/          # テスト
└── README.md
```

## 📜 ライセンス

ISC
